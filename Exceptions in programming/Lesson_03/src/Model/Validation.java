package Model;

public interface Validation {
    boolean isValidGender(String data);
    boolean isPhoneNumber(String data);
    boolean isValidateDate(String data);
    boolean isValidName(String data);

}
